# 复制为 project.env 后修改（project.env 勿提交到 git）
# 优先级低于「export PROJECT_ROOT=...」和 run_pipeline.sh --project-root

export PROJECT_ROOT="/data1/bianzw/my_new_project"
# 若原始数据不在 ${PROJECT_ROOT}/raw，请指定实际路径（hlbw 示例见 script/project.env）
# export RAW_DATA_DIR="/data1/bianzw/project/data_raw"
